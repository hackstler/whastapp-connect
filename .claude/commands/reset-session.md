Borra la sesión de WhatsApp eliminando el directorio de autenticación local.

Ejecuta `bash scripts/reset-session.sh` desde la raíz del proyecto. Confirma al usuario que la sesión ha sido borrada y que la próxima vez que arranque el proyecto deberá escanear el QR.
